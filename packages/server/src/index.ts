// ZeroCoder Server
// This package will contain the FastAPI-equivalent TypeScript server
// using Hono or similar framework.

import { VERSION } from '@zerocoder/shared';

console.log(`ZeroCoder Server v${VERSION}`);
